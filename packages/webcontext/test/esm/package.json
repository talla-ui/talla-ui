{
	"private": true,
	"name": "test-esm",
	"scripts": {
		"pretest": "node setup/copy-lib && tsc -p src",
		"test": "run-p serve watch",
		"watch": "tsc -p src -w",
		"serve": "http-server -o test-esm.html"
	},
	"dependencies": {
		"desk-frame": "../../node_modules/desk-frame",
		"@desk-framework/webcontext": "../../"
	},
	"devDependencies": {
		"http-server": "^14.1.1",
		"npm-run-all": "^4.1.5"
	}
}
